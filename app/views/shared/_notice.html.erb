<% flash.each do |name, msg| %>
  <% if name.to_s == "notice" %>
    <%= content_tag :div, msg, :class => "alert alert-notice vanish" if msg.is_a?(String) %>
  <% else %>
    <%= content_tag :div, msg, :class => "alert alert-#{name}" if msg.is_a?(String) %>
  <% end %>
<% end %>